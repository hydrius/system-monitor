# ── Collector .env (one copy per machine) ─────────────────────────────────────
DB_HOST=localhost
DB_PORT=5432
DB_NAME=sysstats
DB_USER=aquitard
DB_PASSWORD=ATIckler1
DB_SSLMODE=prefer

# Override hostname if needed (defaults to socket.gethostname())
# COLLECTOR_HOST=nebula

CPU_INTERVAL=1
DISK_INTERVAL=60
