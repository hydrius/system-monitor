# ── Collector .env (one copy per machine) ─────────────────────────────────────
DB_HOST=192.168.50.77
DB_PORT=5432
DB_NAME=system_stats
DB_USER=aquitard
DB_PASSWORD=ATIckler1
DB_SSLMODE=prefer

# Override hostname if needed (defaults to socket.gethostname())
# COLLECTOR_HOST=hp

CPU_INTERVAL=1
DISK_INTERVAL=60
